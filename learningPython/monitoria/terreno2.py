def terreno (x, y, modificação):
	if matriz [x][y] == 0
    else
        para i = x-1 até i = x+1 faça
            if i >= 0 e i <= 200 então
                if j >= 0 e j<=200 então
                    if i != x e j != y entao
                        if condição == “+” então 
                            matriz [i][j] = matriz [i][j] + 1
                        else 
                            matriz [i][j] = matriz [i][j] – 1