// criando essa tabuada só pra treinar aninhamento de for
#include <stdio.h>

int main(){
	int num, result;
	
	printf("\nVamos imprimir a tabuada de 1 até o número _?:");
	scanf("%d", &num);
	
	return 0;
}
